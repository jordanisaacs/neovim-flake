existing:
- Package and use https://github.com/nvim-telescope/telescope-fzf-native.nvim
- Better installation of treesitter packages
- Only use ripgrep if needed
- sqlfluff format: not applying (sent github discussion)
- keep documenting capabilities

new:
- add onedark.nvim colorscheme
- gitsigns.nvim (git things)
- nvim-neoclip.lua (clipboard history)
- kommentary (better commenting)
- markdown-preview.nvim (powerful preview -> investigate how it works with nixos)
